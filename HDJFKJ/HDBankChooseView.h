

#import <UIKit/UIKit.h>

@interface HDBankChooseView : UIView


@property (nonatomic, strong) UIButton * leftButton;

@property (nonatomic, strong) UIButton * rightButton;

@property (nonatomic, strong) UILabel * bottomLine;

- (void)createSubView;
@end
